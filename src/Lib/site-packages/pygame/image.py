

def load(filename, namehint=""):
    # filename is a url or a file object
    #get image..

    _canvas=html.CANVAS()
    _ctx=_canvas.getContext('2d')

    _ctx.drawImage(filename)

    return _canvas

def save():
    raise NotImplementedError()

def get_extended():
    return False

def tostring(Surface, format, flipped=False):
    return Surface.toDataURL('image/%s' % format)

def fromstring(string, size, format, flipped=False):
    _img = html.IMG(width=size[0], height=size[1])
    _img.src=string

    _canvas=html.CANVAS(width=size[0], height=size[1])
    _ctx=_canvas.getContext('2d')
    _ctx.drawImage(_img,0,0)

    return _canvas

frombuffer=fromstring
